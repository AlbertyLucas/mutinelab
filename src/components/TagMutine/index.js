import styled from 'styled-components';

const TagMutine = styled.h2`
    display: inline-block;
    border-radius: 4rem;
    font-size: 50rem;
    background-color: var(--color-front-end);
    padding: 20rem 18rem;
    color: var(--color-gray-light);
    
`;

export default TagMutine;