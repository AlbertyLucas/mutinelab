import styled from 'styled-components';

const LinkMutine = styled.a`
    text-decoration: none;
    font-weight: bold;
    color: var(--color-primary-medium);
    cursor: pointer;
`;
export default LinkMutine;
